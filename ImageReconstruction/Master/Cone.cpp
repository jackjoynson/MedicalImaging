#include "Cone.h"

Cone::Cone(double startX, double startZ, double xImagePlane, double yImagePlane, double halfAngle)
{
	SetStartX(startX);
	SetStartZ(startZ);
	SetXImagePlane(xImagePlane);
    SetYImagePlane(yImagePlane);
	SetHalfAngle(halfAngle);
}
